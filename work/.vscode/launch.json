{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Server debug",
      "program": "${workspaceFolder}/dist/app.js",
      "cwd": "${workspaceFolder}/dist"
    }
  ]
}